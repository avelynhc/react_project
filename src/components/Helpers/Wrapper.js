const Wrapper = props => {
    return props.children; 
    // children holds all the components you are passing between opening and closing tag of your custom components(Wrapper components)
}
export default Wrapper;